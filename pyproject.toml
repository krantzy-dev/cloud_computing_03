[project]
name = "cloud-computing-03"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.135.2",
    "pandas>=3.0.1",
    "pydantic>=2.12.5",
]
